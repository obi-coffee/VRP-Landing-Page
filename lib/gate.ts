// Soft password gate for the pre-launch site.
// The password itself ships in the server bundle only (API route + middleware).
export const GATE_COOKIE = 'tast_vrp_gate'
export const GATE_PASSWORD = 'canikickit'
// Opaque token stored in the cookie once the password has been entered.
export const GATE_TOKEN = 'q-tip-approved-5ft-assassin'
